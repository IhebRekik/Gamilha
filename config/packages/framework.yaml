framework:
    secret: '%env(APP_SECRET)%'

    # CSRF protection
    csrf_protection:
        enabled: true

    session: true

when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file
