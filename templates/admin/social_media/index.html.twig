{% extends 'base.html.twig' %}

<<<<<<< HEAD
{% block title %}Gamelha - Social Media Monitoring{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="text-white">
            <i class="bi bi-share-fill text-primary me-2"></i>Social Media Index
        </h1>
        <a href="{{ path('admin_social_media_new') }}" class="btn btn-success shadow-sm">
            <i class="bi bi-plus-lg me-1"></i> Créer un post
        </a>
    </div>

    <div class="card bg-dark border-0 shadow-lg">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-dark table-hover align-middle mb-0">
                    <thead class="text-secondary text-uppercase small border-bottom border-primary border-opacity-25">
                        <tr>
                            <th class="ps-4">ID</th>
                            <th>Plateforme</th>
                            <th>Contenu</th>
                            <th>Engagement (Likes)</th>
                            <th class="text-end pe-4">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for sm in social_media %}
                        <tr class="border-bottom border-secondary border-opacity-10">
                            <td class="ps-4 text-secondary">#{{ sm.id }}</td>
                            <td>
                                {# Badge dynamique selon la plateforme #}
                                {% set badge_color = 'bg-primary' %}
                                {% if 'Twitch' in sm.platform %}{% set badge_color = 'bg-info' %}{% endif %}
                                {% if 'Discord' in sm.platform %}{% set badge_color = 'bg-secondary' %}{% endif %}
                                
                                <span class="badge {{ badge_color }} border border-light border-opacity-25 px-3 py-2">
                                    {{ sm.platform }}
                                </span>
                            </td>
                            <td class="text-truncate" style="max-width: 250px;">
                                {{ sm.content|slice(0, 50) ~ (sm.content|length > 50 ? '...') }}
                            </td>
                            <td>
                                <i class="bi bi-heart-fill text-danger me-2"></i>{{ sm.likes }}
                            </td>
                            <td class="text-end pe-4">
                                <div class="btn-group shadow-sm">
                                    <a href="{{ path('admin_social_media_show', {'id': sm.id}) }}" class="btn btn-sm btn-outline-info">
                                        <i class="bi bi-eye"></i>
                                    </a>
                                    <a href="{{ path('admin_social_media_edit', {'id': sm.id}) }}" class="btn btn-sm btn-outline-warning">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="5" class="text-center py-5 text-secondary">
                                <i class="bi bi-cloud-slash fs-1 d-block mb-2"></i>
                                Aucun enregistrement trouvé dans la base Gamelha.
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
{% endblock %}
=======
{% block title %}SocialMedia index{% endblock %}

{% block body %}
    <h1>SocialMedia index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Platform</th>
                <th>Content</th>
                <th>Likes</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for social_media in social_media %}
            <tr>
                <td>{{ social_media.id }}</td>
                <td>{{ social_media.platform }}</td>
                <td>{{ social_media.content }}</td>
                <td>{{ social_media.likes }}</td>
                <td>
                    <a href="{{ path('admin_social_media_show', {'id': social_media.id}) }}">show</a>
                    <a href="{{ path('admin_social_media_edit', {'id': social_media.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_social_media_new') }}">Create new</a>
{% endblock %}
>>>>>>> origin/Gestion_Vedio_List
